services:

  ivy_apache:
    build: .
    container_name: ivy_apache
    environment:
      APACHE_DOCUMENT_ROOT: /var/www/public
    ports:
      - "${SERVER_PORT:-8080}:80"
    volumes:
      - ../:/var/www
      - ./sites-available:/etc/apache2/sites-available
      - ./sites-enabled:/etc/apache2/sites-enabled
      - ./php/custom.ini:/usr/local/etc/php/conf.d/custom.ini

  ivy_node:
    image: node:latest
    container_name: ivy_node
    working_dir: /var/www
    ports:
      - "${VITE_PORT:-5173}:5173"
    volumes:
      - ../:/var/www
    command: tail -f /dev/null

  ivy_mysql:
    image: mysql
    container_name: ivy_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ${DB_DATABASE:-ivy_db}
      MYSQL_USER: ${DB_USERNAME:-ivy_db_username}
      MYSQL_PASSWORD: ${DB_PASSWORD:-ivy_db_password}
    ports:
      - "${DB_PORT:-3306}:3306"
    volumes:
      - ivy_mysql_data:/var/lib/mysql
      - ./mysql/ivy.sql:/docker-entrypoint-initdb.d/ivy.sql

  ivy_mailcatcher:
    image: dockage/mailcatcher
    container_name: ivy_mailcatcher
    ports:
      - "1080:1080" # Web UI
      - "${MAIL_PORT:-1025}:1025" # SMTP

volumes:
  ivy_mysql_data:
